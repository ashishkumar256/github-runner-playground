name: Deploy

on:
  push:
    tags:
      - 'v*'      # v1.0.0, v1.2.3, etc.
      # or use '*' for all tags

jobs:
  deploy:
    runs-on: infra

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show tag
        run: |
          echo "Tag: ${{ github.ref_name }}"
